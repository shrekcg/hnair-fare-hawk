# 海航监控 · 改动说明与使用指南

> 回答两个问题：**改了什么**（相对原始 Skill 包）和 **你的使用方式有没有变**。
> 更新时间：2026-09-02

## 一、改动清单（相对原始 Skill 包）

| 提交 | 改动 | 文件 | 对你的影响 |
|---|---|---|---|
| `c21d15c` | 初始化：项目模板 + 审阅记录落盘 | 全部 | 无（初始状态） |
| `8e0b791` | **PLUS 端点修正**：PLUS 查询强制使用 `ffl/airLowFareSearch`，不再沿用抓包抓错的 `airCtLowFareSearch`（那个接口对 PLUS 恒返回“无航班”） | `backend/fetcher.py` | ✅ 正向：PLUS 从查不到价 → 稳定出价 JD5290/HU7397 199 元 |
| `67086af` | **优化批次1**（9 项，见下节） | `daemon.py`、`fetcher.py`、`state.py`、`app.py`、`start_all.sh`、`stop_all.sh` | 使用方式基本不变，安全性/稳定性提升 |
| `b54c99d` | 文档：更新进度与启动方式 | `PROJECT_NOTES.md` | 无（文档） |
| `e3a14dd` | 文档：记录普通票价实测结论（F4） | `REVISION_NOTES.md`、`PROJECT_NOTES.md` | 无（文档） |
| `3d2082b` | **签名算法对齐官方**（`_makeSign` 逆向）+ stime 刷新重签兜底 | `backend/fetcher.py`、`config.example.json`、`app.py`、`tests/test_fetcher.py` | 默认零变化；`sign_refresh` 可选项 |
| 本次 | **控制台 UI 重写**：Streamlit → React+Vite（顶部导航+卡片+Tab），API 层为 `web_api.py` | `web/`（新）、`web_api.py`（新）、`start_all.sh`、`stop_all.sh`、`tests/test_web_api.py` | 界面焕新；发送测试消息改为用已保存的 SendKey；空 SendKey 不再覆盖旧值 |

### 优化批次1 明细（`67086af`）

| # | 改动 | 效果 |
|---|---|---|
| 1 | Web 启动固定绑定 `127.0.0.1` | 局域网内其他设备**不再能打开**你的监控页面（原来可以，属安全修复） |
| 2 | 任务间随机错峰 10–30s | 多任务时请求不再毫秒级连发，降低风控特征 |
| 3 | 凭证失效按任务指数退避 5min→60min | token 失效后不再每轮硬撞，且不中断其他任务 |
| 4 | 结果分类（ok/network/parse/empty）+ 日志区分 | 「真没票 / 网络错 / 解析错 / 被限流」日志里能分清 |
| 5 | 价格历史 `price_history.jsonl`（5MB 轮转） | 页面可看最近 50 条历史，不再查完即丢 |
| 6 | 自适应轮询频率 | 有价 → 间隔减半（不下探下限）；无价 → ×1.5（封顶夜间 2 倍） |
| 9 | 可选代理 `config.proxy` | 需要在代理环境跑时可配（页面高级设置） |
| 10 | 日志轮转（>1MB 保留 1 份） | run_log.txt 不再无限增长 |
| 11 | 任务级启停按钮 | 页面上可以单独停用某条任务（原来只能全局启停） |

## 二、你的使用方式有没有变

| 操作 | 原来 | 现在 |
|---|---|---|
| 启动/停止 | —（当初由 agent 手动双命令） | `./start_all.sh` / `./stop_all.sh` 一键 |
| 打开页面 | `http://localhost:8501` | 同样，但**只能用本机访问**（局域网已不可见） |
| 添加任务 | 页面「添加监控任务」 | 不变（票价类型可选「普通票价 / PLUS专享」） |
| 启停单任务 | 无 | 页面「任务列表」下方启停按钮 |
| 换/加票据（cURL） | 无页面入口，需命令行脚本 | **侧边栏「票据管理」** 粘贴 → 保存，下一轮生效 |
| 微信推送、监控时段 | 页面配置 | 不变 |
| 代理 | 无 | 页面高级设置（可选） |

**结论：日常使用只有一处新增入口（票据管理），其余全部是向后兼容的安全/稳定增强，没有需要你改变习惯的操作。**

## 三、票据管理（核心资源）入口说明

Web 页面侧边栏 → **票据管理（抓包请求 cURL）**。

- 两块独立输入：**PLUS 专享票据** 与 **普通票价票据**，各自对应 config 的 `plus_curl` / `normal_curl`。
- 当前状态显示：PLUS ✅ 已配置 ｜ 普通 ❌ 未配置。
- 保存即生效：daemon 每一轮都会重读 config，**无需重启**。
- 抓票位置（两者不同，需各自抓）：
  - PLUS 专享：请求名含 `ffl/airLowFareSearch`
  - 普通票价：请求名是 `airLowFareSearch`
  - 方法：官网查询页 F12 → Network → 筛选此请求名 → 右键 Copy as cURL (bash)；若系统报“无法识别”，改用 Copy as cURL (cmd)。

## 四、签名重签专项的边界（已完成并验证，不会破坏现状）

「stime 刷新 + 重签」（优化项 7）已实现并通过双跑验证：

1. **默认关闭**：新算法已加到代码里（`_make_hnair_sign` 对齐官方前端算法），但 `config.json` 的 `sign_refresh` 开关默认 `false`，行为与现在完全一致；
2. **双跑验证（已做）**：同一请求分别跑「原抓包签名」与「刷新 stime + 新算法重签」，返回价格列表完全一致（JD5290/HU7397 各 ¥199）；
3. **失败回退**：开关打开后一旦遇到 `E00001 验签错误`，自动回退到原抓包签名重发一次，抓取效果不劣于现状。

即：逆向只是**新增一条可选路径**，不替换、不删除任何现有逻辑，抓包签名永远作为底线保留。抓取效果与整套流程工具的使用效果不会因逆向而变差。之后若想打开 `sign_refresh`（建议先跑几天再开），直接改 `config.json` 的 `sign_refresh: true` 即可。