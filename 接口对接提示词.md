# 接口对接提示词（数据沉淀 → 实时监控对话）

> 用法：把下面的【正文】整体复制到另一个对话（实时监控系统那个会话）里发送即可。
> 注意：另一个对话需要先知道这个文件路径及其上下文，如对方无法读取文件，可让对方先 `cd /Users/Wcg/Desktop/Project_local/海航监控` 再按需读取。

---

【正文开始】

# 请为「航班底表」写一个只读查询接口

## 一、背景

我是本项目「数据沉淀」这一块的对话。我已经把两份全量航班计划数据源（sxfroute CSV 航班计划 1594 条 + HNA666 官网随心飞查询快照）合并、校验、裁决，建成了一份**规范化底表**：

- 文件：`data/sediment/flights_normalized.json`（1733 条，2026 秋航季 09-01 ~ 10-24）
- 生成：`scripts/sedimentation/` 里一套 CLI（compare / build / query / review / update），测试全绿
- 更新：`scripts/sedimentation/update.py` 一键拉上游重建，版本记录在 `data/sediment/versions.json`，每次更新前的旧底表会存时点快照（`data/sediment/snapshots/`）

现在请你（实时监控那边）基于这份底表，为监控系统写一个**只读查询接口**，后续我们自己的查询/监控逻辑都通过它拿「哪些航班能飞、哪天有、几点飞」。

## 二、底表数据结构（读这个文件就能拿到）

顶层：
```json
{
  "generated_at": "2026-09-02",
  "count": 1733,
  "note": "...",
  "records": [ ... 每条一个航班 ... ]
}
```

每条 record 的字段（JSON 示例）：

```json
{
  "flight_no": "8L9501",
  "carrier": "祥鹏航空",
  "origin": {"city": "昆明", "airport": "昆明长水国际机场", "iata": "KMG"},
  "dest": {"city": "郑州", "airport": "郑州新郑国际机场", "iata": "CGO"},
  "dep_time": "21:15",
  "arr_time": "23:55",
  "days": [1,2,3,4,5,6,7],
  "days_csv": [1,2,3,4,5,6,7],
  "days_hna": [1,2,3,4,5,6,7],
  "csv_dates": [["2026-09-01","2026-10-24"]],
  "hna_dates": [["2026-10-01","2026-10-24"]],
  "effective_dates": [["2026-10-01","2026-10-24"]],
  "csv_products": ["2666","666"],
  "hna_products": ["2666","666"],
  "product": "666/2666",
  "product_conflict": false,
  "review_decision": null,
  "notes": null,
  "source": "both",
  "raw_csv_lines": ["1280"]
}
```

字段语义（重要）：
- `flight_no`：航班号；`carrier`：航司名。
- `origin.city` / `dest.city`：城市名（查询主要用它）；`origin.iata` / `dest.iata`：三字码（HNA666 侧已用 CN271 表补全，实测 100%）。
- `dep_time`/`arr_time`：`HH:MM`，可能为 null（经停中间段无时刻，来源 HNA666）。
- `days`：周几（1=周一 … 7=周日）的数组，合并视角；`days_csv`/`days_hna` 两源各自视角。
- `effective_dates`：**实际可飞日期区间**，`[起, 止]` ISO 字符串，可多段。这是判断「某天能不能飞」的主依据（HNA666 实际执飞优先、CSV 备注修正兜底）。`csv_dates`/`hna_dates` 是两源各自口径，给接口做诊断用。
- `product`：`"666"` / `"2666"` / `"666/2666"`（并集，宁多勿漏）。**注意：判断「666 档能不能飞」要按 `product` 是否包含 `"666"`，且只能按 `/` 切分后判成员，不能 `"666" in product`（`"666" in "2666"` 是 True 的坑）。**
- `product_conflict`：两源档位判定是否冲突（首跑 44 条已全部裁决，当前全为 false）。
- `source`：`both` / `csv` / `hna`（两源都有 / 仅 CSV 计划 / 仅官网可兑）。
- `raw_csv_lines`：对应 CSV 原始行号，溯源用。

## 三、接口需求

1. **只读**：只读取 `data/sediment/flights_normalized.json`（启动时加载一次进内存即可，1.6MB；或按需懒加载），不得修改底表文件。
2. **查询维度**（都支持组合过滤）：
   - 出发城市 / 到达城市（`origin.city` / `dest.city` 精确匹配；能兼容 IATA 更好）
   - 产品档位：`product`，取值 666 / 2666，语义=「档位**含**该值」（666 时返回 666/2666 双档，因为 666 卡能飞）
   - 日期：`date=YYYY-MM-DD`，命中条件 = 该日期落在 `effective_dates` 任一段 **且** `days` 包含当天星期（可用 `datetime.date.isoweekday()` 转换）
   - 可选的航班号精确过滤、方向（出发/到达二选一）
3. **返回**：JSON 数组，字段尽量贴合底表 record（可直接透传），建议同时返回查询条件与命中条数，方便联调。
4. **实现位置**：由你决定，建议独立小模块（如后端目录下 `flight_table.py` 或类似）+ 导出供现有 daemon/web_api 引用；命名风格跟你们现有代码一致。
5. **复用参考**：`scripts/sedimentation/query.py` 里已有等价查询逻辑（`query()`/`matches_product()`/`matches_date()` 函数，纯函数、无副作用），可以直接复用或移植；那边测试样本内嵌、不依赖大文件。
6. **边界**：不要碰 `config.json` / `tasks.json` / `runtime_state.json` 等敏感或运行时文件；不往底表里写任何东西；接口只做查询，不做余票/价格（那是你们监控的活）。

## 四、交付物

帮我确认以下三点即可：
1. 接口放哪个文件、函数签名是什么；
2. 一个「海口出发 + 666 档 + 2026-10-03」的实际调用示例与返回摘要（条数应为 112 条左右）；
3. 你们调用它查候选航班清单时需要额外什么（比如按天展开执飞日期、按城市聚合），告诉我，我可以配合调整底表或输出格式。

【正文结束】